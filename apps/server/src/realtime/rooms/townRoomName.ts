/**
 * Public, stable Colyseus room name for the (currently empty) TownRoom.
 *
 * Task 018.1 scope: register an empty `TownRoom` under a clear, stable name
 * so future tasks (real client connection, gameplay, persistence) can join it
 * without changing the public name.
 */
export const TOWN_ROOM_NAME = "town";
